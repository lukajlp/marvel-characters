import Loadingbars from "@/components/Loadingbars";
import { FC } from "react";

const loading: FC = ({}) => {
  return (
    <div>
      <Loadingbars className="h-screen" />
    </div>
  );
};

export default loading;
